[project]
name = "aws-dbt-snowflake-project"
version = "0.1.0"
description = "Airbnb Project"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dbt-core>=1.11.11",
    "dbt-snowflake>=1.11.5",
    "sqlfmt>=0.0.3",
]
